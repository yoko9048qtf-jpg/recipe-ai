# requirements.md

> コード・README・実装済みUI文言から読み取れる要件を整理しています。断定できない部分は「（推測）」と明記しています。

## 1. アプリ概要

冷蔵庫にある食材と、その日の気分（ジャンル）を入力すると、条件に合うレシピ候補を提示し、
選んだレシピについて「指定人数分の材料（分量つき）」と「不足している材料（買い物リスト）」、
「作り方」をAIが生成して見せる、モバイル利用を主眼にしたPWA対応 Web アプリ（README.mdに明記）。

トップページのブランド表示は「Sustainable Recipe Maker」（2026-07-05のUI刷新でヘッダーに採用。
食品ロス削減を訴求する「料理サービス／レシピメディア」らしい第一印象を意図したデザイン）。
アプリ内部の識別名（`package.json`, PWAマニフェストの`name`等）は引き続き「AI×レシピメーカー」であり、
本UI刷新ではヘッダー表示文言のみを変更し、アプリの内部名称・ロジックは変更していない。

## 2. 主要機能（実装済み）

1. **食材入力**（3通りの手段を併用可能）
   - 定番食材のチェックボックス選択（卵・乳製品/主食/野菜/肉・魚の4カテゴリ、計26品目）
   - 自由テキスト入力（Enterキーまたは「追加」ボタンでタグ化）
   - 写真アップロード → Claude Vision による食材自動認識
2. **気分（ジャンル）選択**: 和食・洋食・中華・イタリアンの4択
3. **人数指定**: 1〜99人分（未入力・範囲外はレシピ生成をブロック）
4. **レシピ提案**: 楽天レシピAPIの人気ランキングをスコアリング・主菜優先・カテゴリ偏り補正の上で最大10件表示。
   直近表示履歴（最大10件、localStorage）を考慮し、同じレシピばかりが続く体験を緩和
5. **レシピ詳細生成**: 指定人数分の材料（具体的な分量・単位）と調理手順をAIが生成。手持ち食材と照合して
   不足材料（買い物リスト）を算出
6. **出力・共有**:
   - 詳細画面をPDF化してブラウザタブで表示（ダウンロードはしない設計。`html2pdf.js` を動的import）
   - LINEへのテキスト共有（Web Share API対応時はPDFファイル共有、非対応時はテキストリンクにフォールバック）
7. **PWA対応**: マニフェスト・Service Worker（ネットワーク優先＋オフライン簡易キャッシュ）により、
   スマートフォンのホーム画面に追加してアプリのように利用可能

## 3. 画面一覧

`App.tsx` の `view` state（`"input" | "list" | "detail"`）で管理される、単一ページ内の3ビュー構成。

| 画面 | コンポーネント | 概要 |
|---|---|---|
| ① 食材入力画面 | `IngredientInput`（+ `CommonIngredients`, `PhotoUpload`） | 食材・ジャンル・人数を入力して検索 |
| ② レシピ一覧画面 | `RecipeList` | 提案されたレシピをカード一覧表示、タップで詳細へ |
| ③ レシピ詳細画面 | `RecipeDetailView` | 材料・手順・買い物リスト・PDF/LINE共有 |

ヘッダーのアプリタイトルをタップすると常に①へ戻る（`handleGoHome`）。

## 4. 業務フロー（ユーザー視点）

```
① 食材をチェック/入力（写真も可） → ② ジャンルを選ぶ → ③ 人数を入力
→ ④「レシピを探す」→ おすすめレシピ一覧（最大10件）
→ ⑤ 気になるレシピを選択 → 材料・手順が自動生成される
→ ⑥ 買い物リスト（不足材料）を確認 → 必要ならPDF表示 or LINE共有
```

## 5. 制約条件（コード上で明示されているもの）

- **人数未入力ではレシピ詳細を生成しない**（`server/app.js` に明記されたコメント付きの必須ルール。
  1〜99の整数以外は 400 エラー）
- **食材が1件も指定されていない場合は検索不可**（`/api/recipes` は 400 エラーを返す。
  クライアント側も検索ボタンを `disabled` にして未然に防止）
- **調味料は常備品として扱う**（醤油・味噌・塩・砂糖・酢・みりん・油・ケチャップ・マヨネーズ等は
  「すべてある前提」とし、不足材料の判定対象から除外する。`PANTRY_EXACT` / `PANTRY_INCLUDES` で定義）
- **表記ゆれの吸収**: にんじん/人参/ニンジン、卵/たまご/玉子等、20種類の同義語グループで正規化して照合
- **API通信回数を増やさない**（履歴除外機能を追加する際に採用された制約。`docs/architecture.md` 3.1参照）
- **楽天API・Claude APIキーが未設定でもサーバー自体は起動できる**（呼び出し時に初めて例外を投げる遅延初期化）
- **Vercelデプロイ時のタイムアウト制約**（README記載、（推測ではなく明記）: Hobbyプランは10秒、
  `vercel.json` の `maxDuration: 30` はPro以上でのみ有効）
- **リクエストボディの上限**: 写真がbase64で送られるため `express.json({ limit: "12mb" })` に拡張済み。
  ただしVercel側のプラットフォーム上限（既定約4.5MB）は超えられない（README記載の既知の制約）

## 6. 非機能要件（推測）

- （推測）ユーザー認証・アカウント機能は存在しない。誰でも即座に使えることを重視した設計と考えられる
- （推測）多人数・高頻度アクセスは主眼ではなく、個人〜家庭利用規模を想定（楽天APIのレート制限に
  頻繁に当たる場合の対策がREADMEで「将来的な検討事項」として触れられている程度のため）
- （推測）日本語利用者専用（UI文言・食材名の正規化・エラーメッセージがすべて日本語固定で、
  国際化(i18n)の仕組みは存在しない）
